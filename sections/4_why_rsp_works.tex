%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Chapter 4. Why RSP Works
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Theoretical Analysis: Why RSP Works}\label{sec:why_rsp_works}

Our theoretical goal is narrower than a theorem about the full nonlinear transformer. We want a mechanism that explains the three empirical regularities from Section~\ref{sec:experimental_analysis}: random continuous prompts can alter the model's earliest reasoning decisions even though they are not optimized for the task; their influence is strongest at the beginning of decoding and weakens later; and this early perturbation can increase trajectory diversity without permanently destabilizing generation. The common control variable throughout the section is $w_{r,i}^{(l)}$, the attention mass assigned to the random tokens. The argument therefore has three parts. First, we characterize what kind of perturbation can open reasoning branches without inserting a hand-crafted prior, and explain why centered isotropic randomness is the relevant choice. Second, we show that attention injects the perturbation through $w_{r,i}^{(l)}$ and simultaneously places an annealing pressure on it as real-token evidence grows. Third, we show why this pre-softmax perturbation can change token rankings in a way that temperature scaling cannot.

% ================================================================
\subsection{Why Random Rather Than a Fixed Token?}\label{sec:why_random_prompt}
% ================================================================

A generic exploration prompt for reasoning should satisfy three structural requirements. It should not systematically pre-commit the model to one semantic direction before the input-dependent computation begins. It should vary across trials, otherwise repeated decoding would trace the same perturbation every time. And because the branch-sensitive direction is unknown in advance and depends on the prompt, the decoding state, and the token comparison, the perturbation should not privilege some directions while leaving others almost untouched. Optimized soft prompts deliberately encode a task prior \citep{kang2025model, xu2025softcot, ye2025thinking, zhang2025memgen}; our question is the complementary one: what perturbation should one use when no such prior is assumed?

A fixed token or a fixed continuous prompt fails this requirement set immediately. It may steer one trajectory, but if the same vector is reused across runs then there is no across-trial exploration at all. Resampling discrete vocabulary tokens restores variability, yet each draw still comes with its own lexical direction and there is no reason for the resulting perturbations to be centered or isotropic. We formalize the choice we make instead.

\begin{definition}[Random Soft Prompt]\label{def:rsp}
Let $\mu_E\in\mathbb{R}^d$ and $\sigma_E^2\in\mathbb{R}_{>0}$ denote the per-coordinate mean and variance of the model's input-embedding distribution. A \emph{Random Soft Prompt} (RSP) of length $L$ is a sequence $\mathbf{H}=(h_1,\dots,h_L)$ with $h_l\stackrel{\text{iid}}{\sim}\mathcal{N}(\mu_E,\sigma_E^2\mathbf{I}_d)$, drawn independently per inference call. Writing $\bar h_l := h_l-\mu_E$, the centered prompt $\bar h_l\sim\mathcal{N}(0,\sigma_E^2\mathbf{I}_d)$ is the formal object analyzed in this section.
\end{definition}

The two design choices in Definition~\ref{def:rsp}---independent resampling and centering---are exactly what produce a centered, isotropic, finite-variance perturbation in prompt space. The downstream attention mechanism then maps this prompt-space distribution to a value-space distribution via the value projection $\mathbf{W}_V^{(l)}$; the analysis below states all variance bounds in this value space (Section~\ref{sec:energy_landscape}).

\begin{lemma}[Semantic neutrality]\label{lem:semantic_neutrality}
Let $\bar{h} \sim \mathcal{N}(0, \sigma^2 \mathbf{I}_d)$ and $\hat{h} := \bar{h}/\|\bar{h}\|$. For any fixed unit vectors $u_1,\dots,u_M \in \mathbb{S}^{d-1}$ and any $\delta \in (0,1)$,
\[
\Pr\!\left[
\max_{r \in [M]} |\hat{h}^{\top} u_r|
\le
\sqrt{\frac{2\log(2M/\delta)}{d-1}}
\right]
\ge 1-\delta.
\]
In contrast, for any nonzero token embedding $e_s$, choosing the anchor direction $u = \hat{e}_s := e_s/\|e_s\|$ yields $|\hat{e}_s^{\top} u| = 1$.
\end{lemma}

Lemma~\ref{lem:semantic_neutrality} gives the precise sense in which the perturbation is neutral. The claim is not that a random vector is literally free of every incidental semantic correlation. The relevant and weaker statement is that it does not systematically align with any pre-selected finite family of semantic alternatives. Centering then removes deterministic first-order drift: under the local surrogate below, the expected perturbation of any logit gap is zero when $\mathbb{E}[\bar{h}] = 0$.

To formalize the exploration part, let $\bar{h}$ be the centered prompt vector and consider the local first-order surrogate
\[
z_i(\bar{h}) \approx z_i + a_i^\top \bar{h},
\qquad a_i := \nabla_{\bar h} z_i(0),
\]
valid for sufficiently small $\|\bar{h}\|$ (Appendix~\ref{app:rsp_noise}); we use this surrogate throughout the section to identify the direction of perturbation rather than to predict its exact magnitude. For a token pair $(i,j)$, the perturbed logit gap becomes
\[
\Delta_{ij}(\bar{h}) \approx \Delta_{ij} + b_{ij}^\top \bar{h},
\qquad
b_{ij} := a_i-a_j.
\]
The relevant question is therefore not ``does the perturbation have variance?'' but ``how much variance does it retain along the unknown direction $b_{ij}$ that matters for this branching decision?'' The theorem below answers that question at the level of second moments.

\begin{theorem}[Direction-agnostic coverage]\label{thm:minimax_directional_coverage}
Let $D$ be any zero-mean perturbation distribution on $\mathbb{R}^d$ with covariance $\Sigma_D$ and variance budget $\mathrm{tr}(\Sigma_D) \le \rho^2$. Define the worst directional variance
\[
\mathcal{E}(D)
:=
\min_{\|b\|=1}
\mathrm{Var}_{h \sim D}(b^\top h).
\]
Then
\[
\mathcal{E}(D)
=
\lambda_{\min}(\Sigma_D)
\le
\frac{\rho^2}{d},
\]
with equality if and only if $\Sigma_D = (\rho^2/d)\mathbf{I}_d$. Hence isotropic covariance is the unique way to equalize first-order perturbation variance across all directions while maximizing the least-covered one.
\end{theorem}

Theorem~\ref{thm:minimax_directional_coverage} is not meant to cast prompting as an adversarial game; its role is simpler. When the branch-sensitive direction is unknown at sample time, isotropy is the unique second-order description that neither favors nor neglects any direction. A fixed prompt has $\Sigma_D = 0$ and therefore creates no across-trial exploration at all. An anisotropic perturbation leaves at least one direction less explored than the isotropic optimum. The Gaussian choice in RSP is the simplest centered, rotation-symmetric family that realizes this covariance while matching the scale of pretrained embeddings. This is the sense in which RSP is not just ``some noise'': it is a resampled, centered, direction-agnostic perturbation.

Lemma~\ref{lem:semantic_neutrality} and Theorem~\ref{thm:minimax_directional_coverage} therefore play complementary roles. The lemma rules out semantic pre-commitment to any fixed anchor set, while the theorem rules out directional under-coverage in the local branch-sensitive geometry. A perturbation that satisfies only the first condition would be neutral but too weak in some directions; a perturbation that satisfies only the second would still inject a hand-chosen semantic prior. RSP is useful because it satisfies both at once.

% ================================================================
\subsection{How Attention Turns a Neutral Perturbation into Controlled Exploration}\label{sec:energy_landscape}
% ================================================================

The previous subsection explains why centered isotropic randomness is the right kind of perturbation. The next question is temporal: why is the perturbation strong enough to matter at the beginning of decoding but weak enough not to derail the rest of the reasoning chain? The formal answer needs three ingredients. First, each transformer block can be viewed as one step of an iterative attractor process; without perturbation, the attractor reached at the final layer is determined by the initial state alone, which identifies the structural source of exploration deficit RSP is designed to address. Second, the attention output admits an exact decomposition into a real-token update and a random-token contribution. Third, the same attention mass that injects the random contribution is structurally forced downward as real-token evidence accumulates, and the perturbation propagates through the feed-forward sublayer with controlled amplification.

\paragraph{Iterative attractor view.}
Following the Modern Hopfield correspondence \citep{ramsauer2021hopfield}, one attention update with key matrix $\mathbf{K}$ and value matrix $\mathbf{V}$ is equivalent to a gradient step on the energy
\[
E_{\mathbf{K}}(\sigma) = -\beta^{-1} \log\!\sum_j \exp(\beta\,\mathbf{k}_j^\top \sigma) + \tfrac{1}{2}\|\sigma\|^2
\]
when $\mathbf{K}=\mathbf{V}$. Appendix~\ref{app:hopfield_descent} states three idealized assumptions \emph{(A1)~$\mathbf{K}=\mathbf{V}$, (A2)~weight-tied attention, (A3)~$L_E$-smoothness with $L_E<2$} under which the unperturbed iterates produce a strictly monotone descent in $E_{\mathbf{K}}$ and the sublevel set $\{\sigma:E_{\mathbf{K}}(\sigma)\le E_{\mathbf{K}}(\sigma^{(0)})\}$ is forward-invariant; in the Hopfield regime where the iterate converges in a small number of steps, the trajectory additionally remains in the connected component containing $\sigma^{(0)}$ (Corollary~\ref{cor:connected_basin}). (A1) and (A2) are not literally satisfied by real transformers; the Hopfield view here is a structural picture of attention-only iteration, not a literal statement about every transformer block. Its role in the argument is to identify why a deterministic forward pass is exploration-poor: it cannot leave the connected component selected by initialization, and temperature scaling only acts after the trajectory has already terminated. We do not use this view to claim formal escape rates for real transformers; we use it to motivate why injecting an isotropic stochastic perturbation at every block is the relevant intervention. Throughout the rest of the section we work with the actual transformer attention; the random values $\mathbf{v}_{r_j}^{(l)}$ inherit a Gaussian distribution induced by the prompt construction in Section~\ref{sec:why_random_prompt} and the value projection $\mathbf{W}_V^{(l)}$. Centered isotropic prompts yield value-space distributions with covariance $\sigma_E^2\mathbf{W}_V^{(l)}\mathbf{W}_V^{(l)\top}$, which is full-rank whenever $\mathbf{W}_V^{(l)}$ is, and remains positive-definite in every direction with $\lambda_{\min}(\mathbf{W}_V^{(l)}\mathbf{W}_V^{(l)\top}) > 0$. We adopt the convention that $\sigma_E^2$ in subsequent bounds refers to a per-coordinate variance scale that already absorbs this projection so that the value-space directional variance is bounded below by $\sigma_E^2$ in every direction; under this convention the directional-coverage property of Theorem~\ref{thm:minimax_directional_coverage} carries to value space, and Appendix~\ref{app:rsp_noise} verifies the embedding-scale statistics empirically.

\paragraph{Exact decomposition.}
When $k$ random vectors are inserted as additional keys and values, the attention update can be written as
\begin{equation}
\mathbf{x}_i^{(l+1)}
=
(1 - w_{r,i}^{(l)}) \, \tilde{\mathbf{x}}_i^{(l+1)}
+
\boldsymbol{\eta}_i^{(l)},
\end{equation}
where $\tilde{\mathbf{x}}_i^{(l+1)}$ is the update produced by the real tokens alone, $\boldsymbol{\eta}_i^{(l)} := \sum_{j=1}^{k} \alpha_{i,n+j}^{(l)} \mathbf{v}_{r_j}^{(l)}$ is the contribution of the random values, and
\[
w_{r,i}^{(l)} := \sum_{j=1}^{k} \alpha_{i,n+j}^{(l)}
\]
is the total attention mass assigned to the random tokens. Appendix~\ref{app:rsp_noise} proves the decomposition and the deterministic bound
\[
\|\boldsymbol{\eta}_i^{(l)}\|
\le
w_{r,i}^{(l)} \max_{j \in [k]} \|\mathbf{v}_{r_j}^{(l)}\|.
\]
This is the structural core of the mechanism: the same scalar $w_{r,i}^{(l)}$ controls how much of the original update is retained and how large the random contribution can be. If $w_{r,i}^{(l)}$ is large, the perturbation can materially change the update; if $w_{r,i}^{(l)}$ is small, the computation collapses back toward the real-token update.

\paragraph{Natural annealing through attention.}
The second structural fact is that $w_{r,i}^{(l)}$ is not arbitrary. It satisfies
\begin{equation}\label{eq:decay_main}
  w_{r,i}^{(l)} \leq \frac{k}{k + n \cdot \exp(\Delta_i^{(l)} / \sqrt{d})},
\end{equation}
where $\Delta_i^{(l)} := \min_{j \in [n]} \mathbf{q}_i^\top \mathbf{k}_j - \max_{j' \in [k]} \mathbf{q}_i^\top \mathbf{k}_{r_{j'}}$ is the real-vs-random logit gap and $n$ is the number of real tokens. This bound yields a clean \emph{sequence-wise dilution effect}: for a fixed gap, every newly generated real token tightens the maximum random-token attention compatible with that state. A similar \emph{layer-wise} decay is expected whenever deeper layers enlarge the real-vs-random gap $\Delta_i^{(l)}$. We intentionally treat that layer-wise statement as a model-side hypothesis rather than as a universal theorem, because it depends on how the model sharpens real-token retrieval with depth. The resulting prediction is simple: the perturbation should be strongest exactly when the model has the least context and many continuations remain open, and it should weaken as evidence accumulates.

\begin{theorem}[Sequence-wise annealing under a fixed logit gap]\label{thm:sequence_annealing}
Fix $k$ and $\Delta_i^{(l)}$. The upper bound in Eq.~\eqref{eq:decay_main} is strictly decreasing in the number $n$ of real tokens. Consequently, cache growth alone imposes a monotone decrease on the largest random-token attention compatible with that logit gap.
\end{theorem}

Theorem~\ref{thm:sequence_annealing} proves a structural dilution effect, not monotone decay of the realized $w_{r,i}^{(l)}$ on every decoding path, because $\Delta_i^{(l)}$ also evolves with state. The layer-wise half is likewise model-specific and must be checked empirically.

\paragraph{Feed-forward propagation under local Lipschitz control.}
The decomposition above governs the attention sublayer. A real transformer block applies a feed-forward sublayer $F^{(l)}$ after attention with residual connections, so the cross-block perturbation depends on how $F^{(l)}$ propagates $\boldsymbol{\eta}_i^{(l)}$. If $F^{(l)}$ is $L^{(l)}$-Lipschitz on a bounded set containing both the perturbed and unperturbed post-attention states, then Appendix~\ref{app:ffn_lipschitz} shows
\[
\|\mathbf{x}_i^{(l+1, \text{RSP})} - \mathbf{x}_i^{(l+1, \text{base})}\|
\le
(1 + L^{(l)})\,\big(\|\boldsymbol{\eta}_i^{(l)}\| + w_{r,i}^{(l)} \|\tilde{\mathbf{x}}_i^{(l+1)}\|\big),
\]
which together with $\|\boldsymbol{\eta}_i^{(l)}\| \le w_{r,i}^{(l)} \max_j \|\mathbf{v}_{r_j}^{(l)}\|$ shows that the per-block perturbation amplitude scales linearly with $w_{r,i}^{(l)}$. The bounded-set assumption holds in practice because LayerNorm constrains the post-attention norm, so the perturbed and unperturbed states traversed during a single forward pass lie in a common bounded ball. Iterating across blocks gives a cumulative bound that remains finite under standard architectural assumptions (LayerNorm and GELU/SwiGLU activations are locally Lipschitz on this ball). Combined with the decay bound on $w_{r,i}^{(l)}$, the random perturbation enters with controlled magnitude at early blocks and shrinks toward later blocks, propagating without exponential blow-up.

\paragraph{Empirical verification.}\label{sec:attention_mass}
We test exactly these model-dependent steps on Qwen2.5-Math-7B under suffix injection. If $w_{r,i}^{(l)}$ really acts as an annealed exploration coefficient, then the realized attention to RSP tokens should decay across reasoning positions and layers, whereas attention to question tokens should not exhibit the same monotone pattern because they remain task-relevant throughout decoding. Figure~\ref{fig:attention_mass} shows precisely this contrast. The figure therefore serves a specific logical role: it validates the model-side sharpening behavior that Eq.~\eqref{eq:decay_main} by itself does not prove.

\begin{figure}[ht]
  \centering
  \includegraphics[width=\textwidth]{img/attention/heatmap_qwen7b_suffix.pdf}
  \caption{Per-token attention mass on Qwen2.5-Math-7B (suffix, 500 MATH-500 problems, each with an independently sampled RSP). The layer and reasoning-position axes are each partitioned into five quantile bins; every cell reports the 500-sample mean per-token attention mass directed to \textbf{(a)} the RSP embeddings and \textbf{(b)} the question tokens within that region. The horizontal axis spans reasoning position from early (left) to late (right) generation steps, and the vertical axis spans layer depth from shallow (top) to deep (bottom). Attention to the RSP embeddings in (a) decays along both axes, while attention to the question tokens in (b) remains comparatively uniform.}
  \label{fig:attention_mass}
\end{figure}

% ================================================================
\subsection{Output Distribution Shift and Exploration}
% ================================================================

The practical consequence of the previous mechanism is branch opening. To connect that mechanism to observable decoding behavior, we now return to the output distribution. The relevant question is not whether RSP merely flattens probabilities, but whether it can make the decoder consider tokens that temperature scaling can never promote into contention.

\paragraph{Why temperature is insufficient.}
Temperature scaling transforms probabilities as $p_i^{(\tau)} \propto p_i^{1/\tau}$ and preserves token ranking, i.e., $p_i > p_j \iff p_i^{(\tau)} > p_j^{(\tau)}$. It can soften or sharpen confidence, but it cannot create a new ordering among candidate tokens at a fixed decoding state.

\paragraph{Why RSP changes rankings.}
RSP acts before softmax, through the hidden state. At a fixed decoding state, temperature rescales existing logit gaps, whereas RSP perturbs the gaps themselves. Using the same first-order local surrogate as above, we write $z_i(\bar{h}) \approx z_i + a_i^\top \bar{h}$ for the centered prompt $\bar{h}$. The next two propositions should therefore be read as statements about this local surrogate, which is sufficient for the comparison to temperature because temperature is rank-preserving regardless of approximation.

\begin{proposition}[Stochastic ranking]\label{prop:stochastic_ranking}
If there exists a token pair $(i,j)$ such that $a_i \neq a_j$, then
\[
0 < \Pr_{\bar{h}}\big(z_i(\bar{h}) > z_j(\bar{h})\big) < 1.
\]
Thus, unlike temperature scaling, RSP randomizes token rankings at each decoding step.
\end{proposition}

Proposition~\ref{prop:stochastic_ranking} is the minimal statement: pairwise order can flip. Exploration requires a stronger conclusion, namely that tokens outside the baseline candidate set can actually enter it.

\begin{proposition}[Top-$K$ entry under a nonempty entry region]\label{prop:support_expansion}
Let $J_K(s_t)$ be the baseline top-$K$ set at decoding state $s_t$, and let $i \notin J_K(s_t)$. Define
\[
\mathcal{G}_{i,K}
:=
\left\{
\bar{h} \in \mathbb{R}^d :
\#\left\{j \neq i : z_j + a_j^\top \bar{h} > z_i + a_i^\top \bar{h}\right\}
\le K-1
\right\}.
\]
If $\mathcal{G}_{i,K}$ contains a nonempty open set and $\bar{h}$ is sampled from an isotropic Gaussian, then
\[
\Pr_{\bar{h}}\big(i \in \mathrm{TopK}(p^{\mathrm{RSP}}(\cdot \mid s_t, \bar{h}))\big) > 0.
\]
\end{proposition}

Proposition~\ref{prop:support_expansion} is intentionally conditional. It does not claim that every excluded token admits such a top-$K$ entry region. It states that whenever the first-order model contains an open region in prompt space where a previously excluded token moves into the top-$K$, isotropic perturbations reach that region with positive probability. A useful sufficient condition for the open-region hypothesis is the following: if the linear coefficients $\{a_j-a_i\}_{j\in J_K(s_t)\setminus\{i\}}$ are in general position in $\mathbb{R}^d$ and at least $K$ of them are not Pareto-dominated by $a_i$ (i.e.\ for each such $j$ there exists $\bar h_j$ with $(a_j-a_i)^\top \bar h_j < z_i-z_j$), then $\mathcal{G}_{i,K}$ contains a nonempty open polyhedron, since it is defined by strict linear inequalities whose feasible set has nonempty interior under linear independence. Generic configurations of $\{a_j\}$ in high ambient dimension $d\gg K$ satisfy this condition. Section~\ref{sec:diversity} verifies empirically that the entry regions are nonempty in the regime of interest. Temperature cannot reach $\mathcal{G}_{i,K}$ because it never changes token order at all; the mechanism here is branch opening, not mere probability flattening.

\begin{corollary}[Sample complexity of top-$K$ admission]\label{cor:sample_complexity}
Let $\mu_i := \Pr_{\bar{h}}(\bar{h} \in \mathcal{G}_{i,K}) > 0$ denote the per-sample admission probability for token $i$. Within $N$ independent RSP samples, the probability of at least one admission is at least
\[
1 - (1-\mu_i)^N \;\ge\; 1 - \exp(-N \mu_i).
\]
\end{corollary}

Corollary~\ref{cor:sample_complexity} closes the loop from per-sample positive probability to multi-trial exploration. Even when $\mu_i$ is small, $N \cdot \mu_i = \Theta(1)$ samples suffice to admit a previously excluded token with constant probability. This is the formal bridge from the static guarantee of Proposition~\ref{prop:support_expansion} to the trajectory-level Pass@$N$ phenomena reported in Section~\ref{sec:diversity}.

\paragraph{From rank flips to trajectory diversity to accuracy gain.}
Together, Propositions~\ref{prop:stochastic_ranking} and~\ref{prop:support_expansion} close the logical chain from Section~\ref{sec:entropy} to Section~\ref{sec:diversity}, up to the local-surrogate assumption above. Early RSP perturbations alter logit gaps before softmax, which can admit previously excluded tokens into the candidate set. Because autoregressive decoding compounds early choices, one such early admission is enough to push the trajectory into a different reasoning cluster even if later perturbations are already small. RSP therefore does not need to perturb the entire chain of thought uniformly: it only needs to alter the first few branching decisions before attention-mediated attenuation suppresses further noise, which is why early exploration and late stabilization coexist in the same mechanism.

The connection from trajectory diversity to accuracy gain requires a property of reasoning tasks themselves: the existence of multiple valid trajectories. We formalize this as a separate proposition rather than absorb it into the perturbation analysis.

\begin{proposition}[Exploration-meets-multi-path accuracy gain]\label{prop:multi_path}
Fix a problem $x$. Let $\mathcal{T}^\star(x) \subseteq \mathcal{T}(x)$ denote the (nonempty) set of trajectories that reach a correct answer, and let $p_{\mathrm{base}}(x)\in\{0,1\}$ denote the deterministic-baseline indicator $\mathbf{1}[\text{baseline trajectory}\in\mathcal{T}^\star(x)]$. Assume that under one RSP sample the probability of producing some trajectory in $\mathcal{T}^\star(x)$ is at least $p_{\min}(x) > 0$. Then under $N$ independent RSP samples,
\[
\Pr\!\big(\text{Pass@}N \text{ is correct on } x\big)
\;\ge\;
\max\!\left\{p_{\mathrm{base}}(x),\;1 - (1-p_{\min}(x))^N\right\}
\;\ge\;
1 - \exp(-N\,p_{\min}(x)).
\]
The expected accuracy gain over the deterministic baseline is therefore at least $\sum_{x}(1 - p_{\mathrm{base}}(x))\big(1 - \exp(-N p_{\min}(x))\big) / |\mathcal{D}|$, summed over a test set $\mathcal{D}$.
\end{proposition}

\begin{proof}
The Pass@$N$ event is satisfied if the baseline is correct (probability $p_{\mathrm{base}}(x)$, which is $0$ or $1$) or if any of $N$ independent RSP samples produces a correct trajectory. Independence of samples gives the second probability $1-(1-p_{\min}(x))^N$. Taking the maximum yields the displayed lower bound. The exponential bound is $1-x\le e^{-x}$. The expected gain is the per-problem improvement integrated over $\mathcal{D}$, restricted to problems where the baseline fails ($p_{\mathrm{base}}(x)=0$).
\end{proof}

Proposition~\ref{prop:multi_path} converts the local guarantees of Propositions~\ref{prop:stochastic_ranking} and~\ref{prop:support_expansion} into a global accuracy claim, but only via the auxiliary assumption that $p_{\min}(x) > 0$ at the trajectory level. That assumption is plausible whenever (i) some trajectory in $\mathcal{T}^\star(x)$ differs from the baseline at a decoding step where Proposition~\ref{prop:support_expansion} guarantees a positive-measure entry region for one of its tokens, and (ii) downstream attention dynamics allow the corresponding RSP-perturbed prefix to extend into a correct completion. Both (i) and (ii) are model- and dataset-dependent. Section~\ref{sec:diversity} therefore plays a load-bearing empirical role: it verifies that RSP-induced trajectories include both improvements and regressions over the baseline, with net gain emerging under repeated sampling. The mechanism is exploration meeting multi-path structure, not steering toward correctness.

Taken together, the causal chain is now explicit. Randomness matters because RSP is resampled, centered, and direction-agnostic; attention turns that perturbation into an early exploration coefficient and then anneals it; the resulting early rank flips open trajectories that temperature scaling alone cannot reach; and the multi-path structure of reasoning converts trajectory diversity into accuracy gain under repeated sampling. Section~\ref{sec:experimental_analysis} therefore reports four observable faces of a single mechanism rather than four disconnected phenomena.
